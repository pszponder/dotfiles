# Omarchy owns global CLI tools through pacman/omarchy pkg.
# Keep only tools that are not supplied by Omarchy and are intentionally
# managed in the user's mise environment.

[tools]
nub = "latest"
ruff = "latest"
ty = "latest"
uv = "latest"
