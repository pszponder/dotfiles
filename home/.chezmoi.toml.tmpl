[data]

git_name = "{{ .git_name }}"
git_email = "{{ .git_email }}"
machine_type = "{{ .machine_type }}"

# automatic system values
os = "{{ .chezmoi.os }}"
hostname = "{{ .chezmoi.hostname }}"
arch = "{{ .chezmoi.arch }}"