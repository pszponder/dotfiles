{{- if eq (.machine_type | default "workstation") "server" -}}
#!/usr/bin/env bash
set -euo pipefail

command_exists() { command -v "$1" >/dev/null 2>&1; }

# Add server-specific package installations here

echo "✅ Server package installation complete."
{{- end }}
