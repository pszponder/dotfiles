# Using Mise Installed Languages

## Configuring JetBrains Rider w/ dotnet from mise

1. Navigate to `Build, Execution, Deployment` > `Toolset and Build` > `Toolset`
2. In `.NET CLI executable path:`, select the path to dotnet
	1. `/home/piotr/.local/share/mise/installs/dotnet/latest/dotnet`