# scripts/config.env
#
# Optional environment variables shared by scripts in this repo.
#
# How scripts should use this file:
# - A script may source this file (if present) to pick up environment variables.
# - Keep it as simple bash assignments: VAR="value".
#
# IMPORTANT:
# - This file is sourced by bash. Keep it simple: VAR="value" assignments.
# - Do not put secrets here in a public repo.

# ---------------------------------------
# --- Kanata scripts defaults (macOS) ---
# ---------------------------------------
# Used by:
# - scripts/mac_kanata_install.sh
# - scripts/mac_kanata_uninstall.sh

# Launchd / file locations
LAUNCHD_LABEL_PREFIX="com.pszponder.kanata"
PLIST_DIR="/Library/LaunchDaemons"
LOG_DIR="/Library/Logs/Kanata"

# Kanata runtime
KANATA_CFG="$HOME/.config/kanata/config.kbd"
KANATA_PORT=""
KANATA_EXTRA_ARGS=""

# DriverKit pkg install
# If empty, install script uses GitHub API to discover latest .pkg.
DRIVERKIT_PKG_URL=""
# Optional tag pin, e.g. "v6.2.0". If empty, uses latest.
DRIVERKIT_VERSION_PIN="v6.2.0"
# Optional: set if you hit GitHub API rate limits.
GITHUB_TOKEN=""

# DriverKit binary locations (override only if needed)
VHID_MANAGER_BIN="/Applications/.Karabiner-VirtualHIDDevice-Manager.app/Contents/MacOS/Karabiner-VirtualHIDDevice-Manager"
VHID_DAEMON_BIN="/Library/Application Support/org.pqrs/Karabiner-DriverKit-VirtualHIDDevice/Karabiner-VirtualHIDDevice-Daemon"

# Uninstall behavior defaults (clean removal)
REMOVE_KANATA=1
REMOVE_DRIVERKIT=1
REMOVE_LOGS=1
