#!/usr/bin/env bash

# Fedora packages – machine-type aware
packages=()
mt=${MACHINETYPE:-server}
if [[ "$mt" == "server" ]]; then
  # Add packages for server workstation here
  packages=(git curl)
else
  # Add packages for workstation machine here
  packages=(git curl)
fi